{
  "name": "wordpress-require-plugins",
  "version": "1.0.0-beta.1",
  "description": "A drop-in WordPress function, that requires plugin dependencies for custom themes as bespoke admin notices",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "js-cookie": "^3.0.5"
  }
}
